{
  "name": "ladx-custom-gfx-tools",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run copy-wasm && vite",
    "copy-wasm": "node scripts/copy-wasm.js",
    "build": "npm run copy-wasm && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.3.1"
  },
  "dependencies": {
    "bsdiff-wasm": "^0.1.4",
    "image-js": "^0.37.0"
  }
}
